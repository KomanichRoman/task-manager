# Task Manager

Веб-приложение для управления задачами с возможностью создания, просмотра, изменения статуса и удаления задач.

---

## Стек технологий

- **Frontend:** React + Vite  
- **Backend:** Node.js + Express  
- **База данных:** PostgreSQL  
- **Дополнительно:** Python (экспорт задач в CSV)

---

## Требования для запуска

Перед запуском убедитесь, что на вашем компьютере установлено:

- **Node.js** (v18 или выше)  
- **PostgreSQL** (v14 или выше)  
- **Python** (v3.10 или выше)

---

## Примечание для Windows
Если при выполнении команд в терминале (PowerShell) возникает ошибка `Невозможно загрузить файл npm.ps1, так как выполнение сценариев отключено`, используйте префикс `cmd /c` перед каждой командой.

**Пример:**

```bash
cmd /c npm install
```

---

## Установка и запуск

### 1. Распакуйте архив

Распакуйте полученный архив в любую папку на вашем компьютере.  
Откройте эту папку в терминале (или в VS Code).

---

### 2. Настройка базы данных

1. Откройте pgAdmin или psql.
2. Создайте базу данных:

   ```sql
   CREATE DATABASE tasklist;
   ```

3. Подключитесь к базе tasklist и выполните SQL-скрипт из файла командой:

   ```sql
   \i sql/database.sql
   ```

Этот скрипт создаст таблицу tasks со следующими полями:
* id — уникальный идентификатор
* title — название задачи
* description — описание
* status — статус (new / in_progress / done)
* created_at — дата создания

4. В файле backend/src/config/db.js укажите свой пароль от PostgreSQL:

```javascript
password: 'ваш_пароль'
```

---

### 3. Запуск бэкенда

Откройте терминал в папке backend и выполните команды:

```bash
npm install
node server.js
```

Сервер запустится на порту 5000.

### 4. Запуск фронтенда

Откройте новый терминал (не закрывая бэкенд), перейдите в папку frontend и выполните:

```bash
npm install
npm run dev
```

Приложение будет доступно по адресу: http://localhost:5173

### 5. Экспорт задач в CSV (Python)
Откройте терминал в папке python и выполните:

```bash
pip install psycopg2-binary
python export_tasks.py
```

После выполнения в папке python появится файл tasks_export.csv со списком всех задач.

---

## Использование
После запуска фронтенда и бэкенда:
1. Откройте http://localhost:5173 в браузере.
2. Введите название и описание задачи → нажмите "Добавить задачу".
3. Задача появится в таблице.
4. Измените статус через выпадающий список:
- 🆕 Новая
- ⏳ В процессе
- ✅ Выполнена
5. Удалите задачу кнопкой "Удалить".

---

## Структура проекта

```
task-manager/
├── backend/                        # Backend на Node.js + Express
│   ├── server.js                   # Точка входа (запуск сервера)
│   ├── package.json                # Зависимости и скрипты
│   └── src/
│       ├── app.js                  # Настройка Express (маршруты)
│       ├── config/
│       │   └── db.js               # Подключение к PostgreSQL
│       ├── controllers/
│       │   └── taskController.js   # Логика CRUD (GET, POST, PUT, DELETE)
│       └── routes/
│           └── taskRoutes.js       # Маршруты API
│
├── frontend/                       # Frontend на React + Vite
│   ├── src/
│   │   ├── components/
│   │   │   ├── TaskList.jsx        # Логика (API-запросы, состояние)
│   │   │   └── TaskListView.jsx    # Разметка и стили
│   │   ├── App.jsx                 # Главный компонент
│   │   └── main.jsx                # Точка входа React
│   └── package.json                # Зависимости и скрипты
│
├── python/                         # Python-скрипт для экспорта
│   └── export_tasks.py             # Экспорт задач из PostgreSQL в CSV
│
├── sql/                            # SQL-скрипт для создания таблицы
│   └── database.sql
│
├── .gitignore                      # Файлы, исключённые из Git
└── README.md                       # Инструкция по запуску
```
